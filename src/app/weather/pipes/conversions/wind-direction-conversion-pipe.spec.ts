import { WindDirectionConversionPipe } from './wind-direction-conversion-pipe';

describe('WindDirectionConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new WindDirectionConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
