import { TemperatureUnitConversionPipe } from './temperature-unit-conversion-pipe';

describe('TemperatureUnitConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new TemperatureUnitConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
