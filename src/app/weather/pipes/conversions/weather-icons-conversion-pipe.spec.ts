import { WeatherIconsConversionPipe } from './weather-icons-conversion-pipe';

describe('WeatherIconsConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new WeatherIconsConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
